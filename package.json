{
  "name": "eugenelyn.ch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "git submodule foreach git pull origin master && node build.js && surge dist",
    "postinstall": "node postinstall.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "data-doge <eugene@enspiral.com> (http://fuckafucka.com)",
  "license": "ISC",
  "devDependencies": {
    "surge": "^0.19.0"
  }
}
